repos:

- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.15.15
  hooks:
    # Run the linter and fix issues
    - id: ruff
      args: [--fix]
    # Run the formatter (replaces black and isort)
    - id: ruff-format
